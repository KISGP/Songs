name: AutoDeploy
on:
  push:
    branches:
      - main
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v2.3.1
        with:
          persist-credentials: false

      - name: Install and Build 🔧
        run: |
          npm install
          npm run build
      - name: Deploy 🚀
        uses: JamesIves/github-pages-deploy-action@3.7.1
        with:
          ACCESS_TOKEN: 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
          BRANCH: gh-pages
          FOLDER: dist
          CLEAN: true